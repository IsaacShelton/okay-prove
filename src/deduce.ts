
import unimplemented from 'ts-unimplemented';
import { AstExpr } from './ast';

// Returns new facts that can be concluded that are not contained
// within 'premises'
export function deduce(premises: AstExpr[]): AstExpr[] {
    return unimplemented();
}
